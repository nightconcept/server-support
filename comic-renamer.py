import argparse
parser = argparse.ArgumentParser()
parser.add_argument("path", help="path that will be renamed")
args = parser.parse_args()
print(args.path)

# parse out path to iterate

# prompt for name

# replace spaces and invalid characters

# iterate through all files

# categorize each into list of old name, volume

# rename each file into new name